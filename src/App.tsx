import React from 'react';
import 'styles/main.scss';

import Button from 'components/Button';

const App: React.FC = () => {
    return (
        <div>
            <Button>Hello</Button>
        </div>
    );
};

export default App;
